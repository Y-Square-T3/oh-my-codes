name: omc-pi-ci

on:
  push:
    branches: [ main ]

concurrency:
  group: omc-pi-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install

      - name: Typecheck
        run: yarn op typecheck

      - name: Lint
        run: yarn op lint

      - name: Format check
        run: yarn op fmt:check

      - name: Test
        run: yarn op test
