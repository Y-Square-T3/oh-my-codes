.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Whether you use PnP or not, the node_modules folder is often used to store
# build artifacts that should be gitignored
node_modules

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*

.idea

# Build artifacts
dist/

# Platform-specific binaries (built in CI, tracked via .gitkeep)
packages/darwin-arm64/bin/oh-my-codes*
packages/darwin-x64/bin/oh-my-codes*
packages/linux-x64/bin/oh-my-codes*
packages/windows-x64/bin/oh-my-codes*
